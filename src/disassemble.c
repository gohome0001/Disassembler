#include "map.h"

int matchwith()
int isPrefix(const BYTE* p_offset, BYTE** pp_offset) 
{
    BYTE first = p_offset[0];
    if(map[first].) 
}
int sizeofOpcode(BYTE*);
int operandSize(BYTE*);

int disassemble(BYTE*;